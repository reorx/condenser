# Condenser 前端 — Milestone 2 计划 & 待定决策

> 承接 M1(脚手架 + 鉴权/TG 登录 + 时间线无限滚动/滚过即已读/消息渲染/侧边栏/收藏)。
> 本文列出 M2 要做的功能、每项依赖的后端 API、发现的**后端缺口**,以及需要你拍板的**待定决策**。

---

## 1. M2 功能范围(要做什么)

### 1.1 订阅完整管理(spec C2 / D1.3)
当前 `SubscriptionsView` 是只读列表,M2 补齐操作:
- **启用/停用**:`PATCH /api/subscriptions/{id}` `{enabled}` + 乐观更新侧边栏/时间线。
- **取消订阅**:`DELETE /api/subscriptions/{id}`(带确认弹窗)。
- **关键词排除规则 CRUD**:每个频道一个管理面板 —— 列出 `GET …/filters`、添加 `POST …/filters {pattern}`、删除 `DELETE /api/filters/{id}`。加/删后后端会**重算 is_filtered**,前端需 invalidate 受影响的 timeline。
- 用到 shadcn:`dialog`(确认 + 关键词面板)、`switch`(启停)、`dropdown-menu`(行操作)。

> ⚠️ **后端缺口**:**全局**关键词规则(`channel_id IS NULL`,spec C5 明确支持「全局 + 频道专属」)目前**没有 add 端点**(只有 per-channel 的 `POST /api/subscriptions/{id}/filters`)。要做全局规则 UI 需后端新增端点 —— 见 §3 / 决策 D6。

### 1.2 日历组件(spec D1.2 / C2 timeline)
- 选「频道 + 日期」查看某频道某天:`GET /api/timeline?channel_id=&date=YYYY-MM-DD`(Timeline 组件已支持 `date` prop,只差 UI 入口)。
- 有消息的日期高亮/可点:`GET /api/timeline/days?channel_id=`。
- 用 `react-day-picker`(已装)+ shadcn `calendar`/`popover`。放在频道视图顶部或侧边栏底部。

### 1.3 新内容提示(spec D2 / Q2)
- 聚焦/定时轮询 `GET /api/timeline/new?after=<cursor>`,有新内容显示「N 条新消息」悬浮条,点击插入顶部并滚到顶。
- 默认 v1 用**轮询**(Q2),SSE/WebSocket 留后续增强。

> ⚠️ **后端缺口**:`/api/timeline/new` 要传一个「当前最新条目」的 cursor,但 `/api/timeline` 只回 `next_cursor`(往**旧**翻页用),没有暴露「最新条目的 cursor / head cursor」。前端要么 (a) 自己用 `date+id` 复刻 base64 cursor 编码(**与后端实现耦合,脆弱**),要么 (b) 后端在 timeline 响应里加 `head_cursor` 或让 `/timeline/new` 接受 `after_date+after_id` 参数。见决策 D7。

### 1.4 媒体灯箱(spec D3)
- 点缩略图 → 全屏 overlay 看原图(`/api/media/{c}/{m}`,不带 thumb);相册可左右切换。
- 视频:overlay 内 `<video controls>` 流式播放(代理已支持 Range?需确认,见决策 D8)。
- 键盘:Esc 关、←/→ 切图。

### 1.5 批量已读(spec C2 `/api/read/bulk`)
- 「全部标已读」/「某频道标已读」/「某日期前标已读」:`POST /api/read/bulk {channel_id?, before_date?}`。
- 放在视图顶部操作区 + 频道行菜单。

### 1.6 设置 / TG 注销
- TG 注销入口:`POST /api/tg/logout`(目前只有 App「Lock app」,没有断开 Telegram 账号的入口)。
- 放一个简单的 Settings 弹窗/页面(显示登录手机号、TG 注销、App lock)。

### 1.7 打磨项
- 单频道视图隐藏每行重复的频道名(改在顶部显示一次)。
- 加载骨架屏 / 错误 toast(shadcn `sonner`)。
- 列表性能:M1 普通渲染,**若实测卡顿**再上虚拟列表(`react-virtuoso`,Q3),非必做。
- 无障碍 + 键盘快捷键(Power User:`j/k` 上下条、`m` 标已读、`s` 收藏)—— 可选,见决策 D9。

---

## 2. 各功能 ↔ 现有后端 API 对照

| 功能 | 端点(已存在) | 备注 |
|---|---|---|
| 启停/删订阅 | `PATCH/DELETE /api/subscriptions/{id}` | ✅ 齐 |
| 频道关键词 | `GET/POST /api/subscriptions/{id}/filters`、`DELETE /api/filters/{id}` | ✅ per-channel 齐 |
| 全局关键词 | —— | ❌ **缺 add 端点** |
| 日历 | `GET /api/timeline?channel_id=&date=`、`GET /api/timeline/days` | ✅ 齐 |
| 新内容轮询 | `GET /api/timeline/new?after=<cursor>` | ⚠️ **缺 head cursor 来源** |
| 媒体灯箱 | `GET /api/media/{c}/{m}`(`?thumb=1` 缩略图) | ⚠️ 视频 Range 待确认 |
| 批量已读 | `POST /api/read/bulk {channel_id?, before_date?}` | ✅ 齐 |
| TG 注销 | `POST /api/tg/logout` | ✅ 齐 |

---

## 3. M2 可能需要的后端改动(取决于决策)

1. **全局关键词规则端点**(若做全局 UI):新增 `POST /api/filters {pattern, channel_id?=null}` 或 `POST /api/global-filters`,并触发全量 `is_filtered` 重算。
2. **新内容轮询的 cursor**(强烈建议):timeline 响应加 `head_cursor`(最新条目的 cursor),或 `/api/timeline/new` 改接受 `after_date+after_id`。否则前端只能脆弱地复刻 cursor 编码。
3. **频道头像**(若 UI 要显示频道图标):新增头像取流端点(类似 media 代理),否则用首字母/Hash 占位。
4. **entities 富文本**(若不再推迟):telememo 加 `entities` 列 + converter 落库 + DisplayMessage 暴露,前端再做粗体/斜体/code/spoiler。

---

## 4. 待定决策(需要你拍板)

| # | 决策点 | 选项 / 我的默认 |
|---|---|---|
| **D1** | **时区**:telememo 存的 `messages.date` 到底是 UTC 还是本地时间? | M1 当 **UTC**(补 `Z`)。**请确认**;若是本地时间要改 `lib/format.ts`。这是显示正确性问题,优先确认。 |
| **D2** | **entities 富文本**是否在 M2 做? | 默认**继续推迟**到后端补列后。若现在要,需先做 §3.4 后端改动(跨仓库)。 |
| **D3** | **取消订阅**是否提供「连同已入库消息一起删除」选项(spec Q4)? | 默认**只取消订阅、保留消息**(后端现状)。要「连同删除」需后端加端点。 |
| **D4** | **新内容推送**方式 | 默认**前端轮询**(Q2);SSE/WebSocket 留后续。确认轮询间隔(默认聚焦时 ~30s)。 |
| **D5** | **长列表虚拟化**(Q3) | 默认**先不做**,实测卡再上 `react-virtuoso`。 |
| **D6** | **关键词规则范围**:M2 只做频道级,还是也做**全局规则**? | 默认**只做频道级**(后端现成)。做全局需 §3.1 后端端点 —— 要的话我同步改后端。 |
| **D7** | **新内容轮询 cursor 来源**(§1.3/§3.2) | 推荐**后端加 `head_cursor`**(干净)。否则前端复刻编码(脆弱)。你定走哪条;要后端我就一起改。 |
| **D8** | **视频播放**:灯箱内 `<video>` 流式播放,还是先「点击新标签打开」? | 默认**灯箱内 `<video controls>`**;需确认媒体代理是否支持 HTTP Range(否则大视频拖动/seek 不顺,可能要后端补 Range 支持)。 |
| **D9** | **键盘快捷键**(Power User:j/k/m/s 等)是否纳入 M2? | 默认**纳入轻量版**(j/k 导航 + s 收藏)。不要就砍。 |
| **D10** | **主题**:目前**仅暗色**(`<html class="dark">`)。要不要亮色/跟随系统切换? | 默认**保持仅暗色**(阅读器场景暗色更聚焦)。要切换我加一个 toggle。 |
| **D11** | **频道头像**:侧边栏/消息头要不要显示 Telegram 频道头像? | 默认**用首字母/Hash 占位**(零后端改动)。要真头像需 §3.3 后端端点。 |
| **D12** | **搜索**(spec Q5) | 默认 **M2 不做**(v1.1 再说)。 |

---

## 5. 明确不在 M2 范围(对齐 spec 非目标)
- 评论/讨论组展示(Q6,v1.1)、媒体离线持久化(Q11)、关键词正则(Q9,后端已预留)、多账号(Q8)、富链接预览卡片、全文搜索(Q5)。
- Docker 多阶段前端构建 + README 收尾属**打包阶段(spec 步骤 9)**,可在 M2 完成后单独做(后端 `app.py` 已会挂载存在的 `frontend/dist`,本地已可端到端跑)。

---

## 6. 建议的 M2 执行顺序
1. 订阅管理(启停/删除/频道关键词)+ 批量已读 —— 让 app 真正可运营。
2. 日历组件(纯前端,API 现成)。
3. 媒体灯箱(依赖 D8)。
4. 新内容轮询提示(依赖 D7,可能带一个小后端改动)。
5. 设置/TG 注销 + 打磨(骨架屏/toast/单频道隐藏频道名/可选快捷键)。

> 先回 **D1(时区)** 和 **D6/D7/D8**(可能触发后端小改),其余可边做边定。
